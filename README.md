# Games

Standalone HTML games published from this repository.

Each game will live in its own folder with an `index.html` file, for example:

- `soccer/index.html`
- `space-battle/index.html`
- `car-mechanic/index.html`

The repository root `index.html` is the game-selection page.
